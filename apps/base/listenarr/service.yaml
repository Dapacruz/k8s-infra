apiVersion: v1
kind: Service
metadata:
  name: listenarr
  namespace: listenarr
  labels:
    app.kubernetes.io/name: listenarr
  annotations:
spec:
  type: ClusterIP
  ports:
  - port: 8787
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: listenarr

