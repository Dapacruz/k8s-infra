apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: linkding
  namespace: linkding
spec:
  entryPoints:
  - web
  routes:
  - match: Host(`linkding.staging.cruznet.io`)
    kind: Rule
    services:
    - name: linkding
      kind: Service
      namespace: linkding
      port: 9090

