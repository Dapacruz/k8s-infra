config:
  persistence:
    volumeName: seerr-data
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: seerr.cruznet.io
  tls:
  - secretName: seerr-cert
    hosts:
      - seerr.cruznet.io
serviceAccount:
  create: false
  name: seerr

