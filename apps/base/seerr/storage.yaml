apiVersion: v1
kind: PersistentVolume
metadata:
  name: seerr-data
spec:
  storageClassName: seerr-data
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeHandle: seerr-data
    volumeAttributes:
      dataLocality: best-effort
      numberOfReplicas: "1"
      staleReplicaTimeout: "30"
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: "5Gi"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seerr-claim
  labels:
    recurring-job.longhorn.io/source: "enabled"
    recurring-job.longhorn.io/backup-daily-with-weekly-full: "enabled"
spec:
  storageClassName: seerr-data
  volumeName: seerr-data
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "5Gi"

